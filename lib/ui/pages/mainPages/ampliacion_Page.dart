import 'package:flutter/material.dart';

class AmpliacionPage extends StatelessWidget {
  const AmpliacionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: TextField(),
    );
  }
}
